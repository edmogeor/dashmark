label: Wanted
unit: count
chart_group: library
source:
  url: "{url}/api/v3/wanted/missing"
  headers:
    X-Api-Key:
      env: DASHMARK_SONARR_API_KEY
      label: dashmark.metric_api_key
jq: .totalRecords
