label: Active streams
unit: count
chart: step
source:
  url: "{metrics_url}/status/sessions"
  headers:
    Accept: application/json
    X-Plex-Token:
      env: DASHMARK_PLEX_TOKEN
      label: dashmark.metric_token
jq: .MediaContainer.size
