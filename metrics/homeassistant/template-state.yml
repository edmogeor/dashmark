display:
  label: Template State
value:
  kind: state
  default_color: info
parameters:
  template:
    label: Template
    type: json_value
source:
  url: "{metrics_url}/api/template"
  method: POST
  json:
    template:
      parameter: template
extract:
  text: true
