source:
  authentication:
    kind: token
    header: Authorization
    prefix: 'MediaBrowser Token="'
    suffix: '"'
    value:
      env: DASHMARK_JELLYFIN_API_KEY
      label: dashmark.metric_api_key
notes: Requires Jellyfin 12 or later.
charts:
  library: step
