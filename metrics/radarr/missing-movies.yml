label: Missing movies
unit: count
chart: step
source:
  url: "{url}/api/v3/wanted/missing"
  headers:
    X-Api-Key: { env: DASHMARK_RADARR_API_KEY, label: dashmark.metric_api_key }
jq: .totalRecords
