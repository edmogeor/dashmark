display:
  label: Approved requests
  chart: requests
value:
  unit: count
source:
  url: "{metric_source}/api/v1/request/count"
  headers:
    X-Api-Key:
      env: DASHMARK_SEERR_API_KEY
      label: dashmark.metric_api_key
extract:
  jq: .approved
