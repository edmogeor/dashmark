display:
  label: Pending requests
  chart: requests
value:
  unit: count
source:
  url: "{api_url}/api/v1/request/count"
  headers:
    X-Api-Key:
      env: DASHMARK_SEERR_API_KEY
      label: dashmark.metric_api_key
extract:
  jq: .pending
