source:
  headers:
    Accept: application/json
  authentication:
    kind: token
    optional: true
    header: X-Plex-Token
    value:
      env: DASHMARK_PLEX_TOKEN
      label: dashmark.metric_token
charts:
  library: step
