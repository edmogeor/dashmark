label: Pending requests
unit: count
chart: step
source:
  url: "{url}/api/v1/request/count"
  auth:
    type: token
    header: Authorization
    prefix: "Bearer "
    value:
      env: DASHMARK_SEERR_API_KEY
      label: dashmark.metric_api_key
jq: .pending
