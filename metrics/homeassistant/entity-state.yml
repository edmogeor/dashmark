display:
  label: Entity State
value:
  kind: state
  default_color: info
parameters:
  entity_id:
    label: Entity ID
    type: url_component
source:
  url: "{metrics_url}/api/states/{entity_id}"
extract:
  jq: .state
