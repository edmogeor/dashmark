display:
  label: Queue depth
value:
  unit: count
source:
  url: '{url}/api/queue'
  headers:
    X-Api-Key:
      env: DASHMARK_TEST_API_KEY
      label: dashmark.metric_api_key
extract:
  jq: .totalCount
