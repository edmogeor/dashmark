label: Series
unit: count
chart_group: library
source:
  url: "{metrics_url}/api/v3/series"
  headers:
    X-Api-Key:
      env: DASHMARK_SONARR_API_KEY
      label: dashmark.metric_api_key
jq: length
