display:
  label: Issues
  chart: issues
value:
  unit: count
source:
  url: "{metrics_url}/api/v1/issue/count"
  headers:
    X-Api-Key:
      env: DASHMARK_SEERR_API_KEY
      label: dashmark.metric_api_key
extract:
  jq: .total
