display:
  label: Wanted
  chart: library
value:
  unit: count
source:
  url: '{metric_source}/api/v3/movie'
extract:
  jq: '[.[] | select(.monitored and (.hasFile | not) and .isAvailable)] | length'
