display:
  label: Download
  chart: line
value:
  unit: bytes_per_second
source:
  url: "{metrics_url}/jsonrpc"
  method: POST
  json:
    jsonrpc: "2.0"
    method: status
    id: 1
extract:
  jq: .result.DownloadRate
