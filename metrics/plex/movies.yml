display:
  label: Movies
  chart: library
value:
  unit: count
source:
  url: "{api_url}/library/sections"
extract:
  for_each:
    items: '[.MediaContainer.Directory[] | select(.type == "movie") | .key]'
    request:
      url: "{api_url}/library/sections/{item}/all"
    value: '.MediaContainer.size // .MediaContainer.totalSize // 0'
    reduce: sum
