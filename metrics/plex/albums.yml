display:
  label: Albums
  chart: library
value:
  unit: count
source:
  url: "{metric_source}/library/sections"
extract:
  for_each:
    items: '[.MediaContainer.Directory[] | select(.type == "artist") | .key]'
    request:
      url: "{metric_source}/library/sections/{item}/all"
    value: '.MediaContainer.size // .MediaContainer.totalSize // 0'
    reduce: sum
